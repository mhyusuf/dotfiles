- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig
    ~/work/.gitconfig: gitconfig-work

- create:
    - ~/work

- shell:
    - 
      command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
